name: Download All Julia Variants and Upload to a Single Release

on:
  workflow_dispatch:
    inputs:
      versions:
        description: 'Enter Julia versions separated by spaces (e.g., 1.12.5 1.11.3)'
        required: true
        default: '1.12.5'

jobs:
  release_upload:
    runs-on: ubuntu-latest
    steps:
      - name: Download JSON Manifest
        run: |
          curl -L -o versions.json https://julialang-s3.julialang.org/bin/versions.json

      - name: Download all binaries for requested versions
        run: |
          for ver in ${{ github.event.inputs.versions }}; do
            echo "Processing version: $ver"
            mkdir -p "downloads/$ver"
            cd "downloads/$ver"
            urls=$(jq -r --arg v "$ver" '.[$v].files[]?.url' ../../versions.json 2>/dev/null)
            if [ -z "$urls" ]; then
              echo "Warning: version $ver not found, skipping."
              cd ../..
              continue
            fi
            echo "$urls" | while read -r url; do
              filename=$(basename "$url")
              echo "Downloading $filename ..."
              curl -L -o "$filename" "$url"
            done
            cd ../..
          done

      - name: Create or Update Release for each version
        run: |
          for ver in ${{ github.event.inputs.versions }}; do
            if [ ! -d "downloads/$ver" ]; then
              continue
            fi
            echo "Creating/updating release for Julia $ver"
            gh release create "julia-$ver" downloads/$ver/* \
              --title "Julia $ver - All Platforms" \
              --notes "All binaries for Julia $ver" \
              --clobber || true
          done
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
